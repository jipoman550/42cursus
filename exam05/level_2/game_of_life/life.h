#ifndef LIFE_H // 헤더 파일의 중복 포함을 방지하기 위한 헤더 가드(Header Guard) 시작입니다.
#define LIFE_H // 만약 LIFE_H가 정의되어 있지 않다면, LIFE_H를 정의하여 이후의 중복 포함을 막습니다.

# include <stdlib.h> // atoi, calloc, free 함수 사용을 위해 포함합니다.
# include <stdio.h>  // putchar 함수 사용을 위해 포함합니다.
# include <unistd.h> // read 함수 사용을 위해 포함합니다.

#endif // 헤더 가드 끝