# 📖 C++ - Module 05

**Repetition and Exceptions (반복과 예외 처리)**

## 📝 Introduction

이 프로젝트는 C++에서 런타임 오류를 우아하게 처리하기 위한 핵심 메커니즘인 예외 처리(Exception Handling)를 다룹니다. 객체가 스스로의 무결성을 보호하기 위해 유효하지 않은 상태가 되는 것을 거부하고, 발생한 문제를 호출자에게 알리는 `try-catch` 구조와 커스텀 예외 클래스 설계법을 학습합니다.

## 🛠️ General Rules

과제를 수행하며 반드시 준수해야 하는 규칙들입니다.

### 1. Compilation

* **Compiler**: `c++`
* **Flags**: `-Wall -Wextra -Werror`
* **Standard**: `-std=c++98` 플래그를 추가해도 완벽하게 컴파일되어야 합니다.

### 2. Forbidden & Allowed

* **C 함수 사용 금지**: `printf()`, `malloc()`, `free()` 사용 시 **0점** 처리됩니다.
* **STL 제한**: Module 08 이전까지 컨테이너(`vector`, `list`, `map` 등)와 알고리즘 사용이 금지됩니다.
* **사용 금지 키워드**: `using namespace`와 `friend` 사용 시 **-42점** 감점됩니다.
* **Orthodox Canonical Class Form (OCCF)**: 모든 클래스는 반드시 기본 생성자, 복사 생성자, 복사 대입 연산자, 소멸자를 포함해야 합니다.
* *단, 예외 클래스는 이 형식을 따르지 않아도 됩니다.*

---

## 🏛️ Exercise 00: Bureaucrat

가장 작은 톱니바퀴인 `Bureaucrat`(관료) 클래스를 통해 예외 처리의 기초를 다룹니다.

### Objectives

* 1(최고) ~ 150(최저) 등급을 가진 관료 클래스 설계.
* 범위를 벗어나는 등급 설정 시 예외를 던지는(throw) 로직 구현.
* `std::exception`을 상속받은 커스텀 예외 클래스 활용.

### 💡 Deep Dive

#### **Q1. `private`과 `public`은 정확히 어떤 역할을 하나요?**

* **데이터 보호(private)**: `_name`과 `_grade`를 숨겨 외부에서 `tom.grade = 0;`과 같이 규칙을 어기고 직접 수정하는 것을 방지합니다.
* **소통 창구(public)**: 사용자는 공개된 `incrementGrade()` 등을 통해서만 안전하게 등급을 조정할 수 있으며, 이 내부에서 유효성 검사가 이루어집니다.

#### **Q2. `std::string`은 클래스인가요?**

* **그렇습니다**: `std::string`은 메모리 관리를 스스로 수행하는 완성된 클래스입니다.
* **과제에서의 활용**: `const std::string _name;`으로 선언하여, 객체 생성 시 이름을 고정하고 이후 변경할 수 없도록 보장합니다.

#### **Q3. 커스텀 예외 클래스의 문법이 왜 이렇게 특이한가요?**

* **상속과 재정의**: `std::exception`을 상속받아 `virtual const char* what() const throw();`를 오버라이딩합니다.
* **`throw()`의 의미**: "이 함수는 내부에서 예외를 던지지 않는다"는 약속입니다. 이를 통해 예외 메시지를 반환하는 과정에서 또 다른 에러가 발생하는 것을 방지합니다.

#### **Q4. 생성자에서 `: _name(name), _grade(grade)` 형식을 쓰는 이유가 무엇인가요?**

* **멤버 초기화 리스트**: 객체의 몸체(`{}`)가 실행되기 전에 멤버를 초기화합니다.
* **상수(const) 초기화**: `_name`이 `const`이기 때문에, 몸체 안에서의 '대입'이 아닌 초기화 리스트를 통한 '초기화'가 문법적으로 필수입니다.

#### **Q5. `operator<<`를 왜 멤버 함수가 아닌 전역 함수로 만드나요?**

* **왼쪽 피연산자 문제**: `std::cout << bureaucrat`에서 연산자 왼쪽의 주인은 `std::cout`(ostream)입니다.
* **유연성**: 우리가 `std::ostream` 클래스를 수정할 수 없으므로, 외부에서 두 객체가 만났을 때의 행동을 정의해 주는 것입니다.

#### **Q6. 예외가 발생하면 프로그램은 어떻게 동작하나요?**

* **즉시 중단**: `throw`가 실행되는 순간, 그 아래의 로직(출력 등)은 무시되고 즉시 가장 가까운 `catch` 블록으로 점프합니다.
* **무결성 보장**: 생성자에서 예외가 터지면 객체 자체가 만들어지지 않습니다. 즉, "등급이 0인 위험한 관료"가 메모리에 존재할 가능성을 원천 차단합니다.

#### **Q7. `std::cout`과 `std::cerr`의 차이는 무엇인가요?**

* **용도 분리**: 일반적인 정보 출력에는 `std::cout`을, 에러나 예외 상황 전파에는 `std::cerr`을 사용합니다.
* **버퍼링**: `cerr`은 버퍼를 거치지 않고 즉시 출력되므로, 프로그램이 예기치 않게 종료되더라도 에러 로그를 확실히 남길 수 있습니다.

---

## 📋 Exercise 01: Form up, maggots!

`Bureaucrat`이 처리해야 할 '서류(`Form`)' 클래스를 도입하여 객체 간의 상호작용과 예외 전파(Exception Propagation)를 학습합니다.

### Objectives

* 이름, 서명 여부, 서명/실행 요구 등급을 가진 `Form` 클래스 설계.
* 관료가 서류에 서명을 시도하는 `beSigned()`와 `signForm()` 로직 구현.
* 여러 객체가 협력하는 과정에서 발생하는 예외를 적절한 위치에서 처리.

### 💡 Deep Dive

#### **Q1. `Form.hpp`에서 `class Bureaucrat;`과 같은 전방 선언(Forward Declaration)은 왜 하나요?**

* **순환 참조 방지**: `Bureaucrat`은 `Form`을 알고, `Form`은 `Bureaucrat`을 알아야 하는 교착 상태를 해결하기 위함입니다.
* **독립성 및 효율성**: 헤더 파일에서는 이름만 알려주어 참조자(&)나 포인터(*)를 처리할 수 있게 하고, 실제 상세 설계도가 필요한 `.cpp` 파일에서만 상대방의 헤더를 `#include` 하여 컴파일 속도와 독립성을 높입니다.

#### **Q2. `beSigned()`는 `Form`에, `signForm()`은 `Bureaucrat`에 있는 이유가 무엇인가요?**

* **책임의 분리(Responsibility)**: 서류(`Form`)는 자신의 서명 조건이 맞는지 판단하고 상태를 변경할 권한을 가집니다. 관료(`Bureaucrat`)는 서류에 서명을 시도하는 '행위'를 수행하고 그 결과를 보고(성공 메시지 또는 예외 출력)할 책임을 가집니다.
* **객체지향 상호작용**: 주체(관료)가 대상(서류)의 내부 데이터를 직접 고치는 것이 아니라, 메시지(`beSigned`)를 던져 객체 스스로 상태를 바꾸게 하는 것이 올바른 설계입니다.

#### **Q3. 왜 `getGradeToSign()`과 `getGradeToExecute()`를 각각 따로 만드나요?**

* **권한의 세분화**: 현실 세계처럼 서류를 승인(서명)할 수 있는 직급과 실제 업무를 처리(실행)할 수 있는 직급이 다를 수 있기 때문입니다.
* **확장성**: ex01에서는 서명 등급만 체크하지만, ex02로 넘어가면 실행 등급을 사용하여 실제 액션을 수행할 자격이 있는지 추가로 검사하게 됩니다.

#### **Q4. 예외의 전파(Exception Propagation)란 무엇인가요?**

* **발생과 처리의 분리**: `Form::beSigned()`는 조건이 맞지 않으면 예외를 던지기만(throw) 합니다. 이 예외는 함수를 호출한 `Bureaucrat::signForm()`의 `catch` 블록으로 전파되어, 그곳에서 실패 사유와 함께 에러 메시지로 출력됩니다.

#### **Q5. 복사 생성자에서 `const` 멤버 변수를 초기화 리스트로 복사하는 게 가능한가요?**

* **초기화 vs 대입**: `const` 변수는 이미 태어난 후에 값을 바꾸는 '대입'은 불가능하지만, 객체가 생성되는 순간인 '초기화 리스트'에서는 값을 지정할 수 있습니다. 따라서 복사 생성자를 통해 원본 객체의 `const` 속성을 안전하게 물려받을 수 있습니다.

#### **Q6. 이미 서명된 서류에 다시 서명하면 어떻게 되나요?**

* **상태 유지**: 현재 로직상 `beSigned()`는 권한이 충족되면 `_isSigned`를 `true`로 설정합니다. 이미 `true`인 상태에서 다시 호출되어도 여전히 `true`로 유지되며, 등급 조건만 만족한다면 예외 없이 성공 메시지가 출력되는 것이 논리적으로 정상입니다.

---

## 📜 Exercise 02: No, you need form 28B, not 28C...

추상 클래스(Abstract Class)와 상속을 도입하여 실제 업무를 수행하는 구체적인 서류들(`Shrubbery`, `Robotomy`, `Presidential`)을 구현하고 다형성(Polymorphism)을 실습합니다.

### Objectives

* 직접 인스턴스화할 수 없는 추상 클래스 `AForm` 설계.
* 부모의 속성을 상속받아 각기 다른 기능을 수행하는 3종의 서류 구현.
* 서명 여부와 관료 등급을 동시에 확인하여 기능을 실행(`execute`)하는 로직 구축.

### 💡 Deep Dive

#### **Q1. 추상 클래스(`AForm`)는 왜 직접 객체를 생성할 수 없나요?**

* **개념적 설계**: "서류"라는 단어는 추상적인 개념입니다. 현실에서도 "서류 가져와"라고 하면 "어떤 서류(사면 서류? 나무 서류?)"인지 되묻게 됩니다.
* **문법적 강제**: 하나 이상의 순수 가상 함수(`= 0`)를 포함하면 추상 클래스가 되며, 이는 실체가 없는 객체가 생성되어 논리적 오류를 일으키는 것을 원천적으로 방지합니다.

#### **Q2. 가상 소멸자(`virtual ~AForm()`)가 왜 필수인가요?**

* **메모리 누수 방지**: 다형성을 사용할 때 `AForm* f = new RobotomyRequestForm();` 처럼 부모 포인터로 자식 객체를 가리킵니다.
* **동적 바인딩**: 소멸자가 `virtual`이 아니면 `delete f;` 시 부모 부분만 삭제됩니다. `virtual`을 붙여야 vtable을 참조해 실제 객체인 자식의 소멸자까지 안전하게 호출됩니다.

#### **Q3. PDF에서 언급한 "한 가지 방식이 더 우아하다(More elegant)"는 설계는 무엇인가요?**

* **검증과 실행의 분리**: 모든 서류가 공통으로 거치는 '서명 확인' 및 '등급 확인' 로직을 부모 클래스(`AForm`)의 멤버 함수(`verifyExecution`)로 구현하는 방식입니다.
* **장점**: 자식 클래스들은 중복 코드 없이 자신만의 고유 동작(`execute`)에만 집중할 수 있어 코드 유지보수성과 가독성이 비약적으로 향상됩니다.

#### **Q4. 다형성(Polymorphism)과 `virtual`은 어떤 관계인가요?**

* **동적 바인딩**: `virtual` 키워드는 컴파일 타임이 아닌 실행 타임(Runtime)에 호출될 함수를 결정하게 합니다.
* **실전 활용**: `Bureaucrat::executeForm(AForm const & form)`은 상대가 어떤 서류인지 몰라도 부모의 인터페이스(`execute`)를 통해 자식의 정체에 맞는 기능을 이끌어냅니다.

#### **Q5. `const std::string& target` 매개변수는 왜 사용하나요?**

* **효율성(Reference)**: 문자열 전체를 복사하지 않고 주소만 전달하여 메모리와 시간을 절약합니다.
* **안정성(const)**: 함수 내부에서 원본 타겟 이름이 수정되는 것을 방지합니다. 또한, 리터럴 문자열(예: `"home"`)을 직접 전달받기 위해서도 `const &` 형식이 필요합니다.

#### **Q6. `std::rand()`와 `std::time()`은 왜 함께 쓰이나요?**

* **의사 랜덤의 한계**: 컴퓨터의 랜덤은 시드(Seed) 값이 같으면 항상 같은 패턴을 출력합니다.
* **시간 기반 시드**: 프로그램을 실행할 때마다 변하는 `std::time(NULL)` 값을 시드로 주어, `RobotomyRequestForm`의 50% 성공 확률이 매번 다르게 작동하도록 보장합니다.

#### **Q7. 파일 입출력 시 `c_str()`을 쓰는 이유는 무엇인가요?**

* **C++98의 제약**: 과거의 `std::ofstream` 생성자는 `std::string` 객체를 직접 받지 못하고 C 스타일 문자열(`char*`)만 인자로 받을 수 있었습니다. 따라서 `string`을 `const char*`로 변환해주는 `c_str()` 호출이 필수적입니다.

---

## 📁 Directory Structure

* `ex00/`: 관료 클래스와 기본 예외 처리 실습.
* `ex01/`: 서명 가능한 서류(Form) 클래스 도입 및 객체 간 상호작용.
* `ex02/`: 추상 클래스 및 실제 기능을 수행하는 3종 서류 구현.
* `ex03/`: (진행 예정) 서류를 생성하는 인턴 클래스.
